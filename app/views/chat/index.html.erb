<section class="channels-list">
  <h1>Ada Channels</h1>
  <ul>
    <% @channels.each do |channel| %>
      <li>
        <%= link_to channel["name"], chat_new_path(channel: channel["name"]) %>
      </li>
    <% end %>
  </ul>
</section>
